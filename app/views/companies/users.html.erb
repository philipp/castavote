<%= will_paginate @users %>
<table>
	<tr>
		<th>Name</th>
		<th>Admin</th>
	</tr>
	<%= render :partial => 'user', :collection => @users%>
</table>
<%= will_paginate @users %>


<%= link_to image_tag ("back.png", :border => 0, :width => 16, :height => 16), @company %>
